{
  "name": "interesthub",
  "version": "1.0.0",
  "description": "*NOTE: Requires [Node.js](http://nodejs.org/).* * Navigate to app directory * `$ npm install -g bower` * `$ bower install` * `$ python -m SimpleHTTPServer <port-number>`(port-number is 8000 by default) * Go to  `localhost:<port-number>/`",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gökçe Uludoğan",
  "license": "MIT",
  "dependencies": {
    "angular": "^1.6.6",
    "angular-route": "1.6.6",
    "bootstrap": "3.3.7",
    "bootstrap-social": "^5.1.1",
    "font-awesome": "4.7.0",
    "jquery": "3.2.1"

  }
}
